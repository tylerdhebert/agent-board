# Conflict Resolution Skill Pointer

This repo no longer treats conflict resolution as a separate card-owning worker role.

Use the repo-local skill instead:

`[.claude/skills/conflict-resolution/SKILL.md](/C:/Users/Tyler/documents/projects/agent-board/.claude/skills/conflict-resolution/SKILL.md)`

Current protocol:

- The current implementer or card owner keeps ownership of the card.
- Conflict resolution is helper work performed under that owner's lifecycle.
- Do not claim the card, reassign ownership, or use a separate resolver identity just to repair the branch.
- After the branch is repaired, the current owner clears conflict state, records the resolution, and moves status truthfully if needed.

This file remains only as a compatibility pointer for older references.

